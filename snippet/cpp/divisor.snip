snippet divisor

  vector<int> divisor(int n){
    vector<int> ret;
    for(int i=1;i*i<=n;i++){
      if(n%i==0){
        ret.P(i);
        if(i*i!=n) ret.PB(n/i);
      }
    }

    sort(all(ret));

    return ret;
  }


